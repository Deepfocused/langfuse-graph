'use client';
import { useEffect, useState } from 'react';
import type GraphProps from '@/types/types';

export default function Summary({ height = 720 }: GraphProps) {
    return <h1>Hello, summary!</h1>;
}
