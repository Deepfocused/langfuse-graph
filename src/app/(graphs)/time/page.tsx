'use client';
import { useEffect, useState } from 'react';
import type GraphProps from '@/types/types';

export default function Time({ height = 720 }: GraphProps) {
    return <h1>Hello, time!</h1>;
}
